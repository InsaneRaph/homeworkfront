export interface CardScheme {
    id: number;
    createdAt: string;
    updatedAt: string;
    number: string;
    holder: string;
    expirationMonth: number;
    expirationYear: number;
    type: string;
}